    import XCTest
    @testable import ACRoutingService

    final class ACRoutingServiceTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ACRoutingService().text, "Hello, World!")
        }
    }
